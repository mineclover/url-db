#!/bin/bash

# MCP launcher script
DB_PATH="${URL_DB_PATH:-/Users/junwoobang/mcp/url-db/url-db.sqlite}"

# Ensure database exists
if [ ! -f "$DB_PATH" ]; then
    touch "$DB_PATH"
fi

# Run the MCP server
exec /Users/junwoobang/mcp/url-db/bin/url-db -mcp-mode=stdio -db-path="$DB_PATH"